# app — Appload partner portal

The portal Appload's clients (shippers) and transporters (carriers) sign into: their orders, quotes, trips, fleet and partners, on the same database as the admin.

Run it with `pnpm --filter app dev`.

It serves on port 3001 (the admin is 3000, the website 3100).
